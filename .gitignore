node_modules
docs/

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Claude Code session state + worktrees (worktrees are separate checkouts,
# never tracked as embedded git repos in the parent branch)
.claude/

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
