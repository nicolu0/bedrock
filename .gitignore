node_modules
docs/

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Claude Code session state + worktrees (worktrees are separate checkouts,
# never tracked as embedded git repos in the parent branch)
.claude/

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.claude/artifacts/

# AppFolio worker persistent browser session (cookies — never commit)
agent/appfolio/.profile/

# Worker screenshots (may contain real tenant/vendor data)
agent/appfolio/*.png

# AppFolio saved auth state — cookies, login-equivalent. NEVER commit.
agent/appfolio/.state.json

# Agent runtime state — live data the server rewrites constantly, not source.
# (helpers.mjs is code and stays tracked; state/archive/ snapshots stay tracked.)
agent/.imessage-state.json
agent/state/*.json
agent/state/*.jsonl
